export default {
    root: 'src',
    base: './',
    build: {
        outDir: '../dist',
        emptyOutDir: false,
        sourcemap: true,
    },

    server : {
        port: 3000,
        host: true
    }
}